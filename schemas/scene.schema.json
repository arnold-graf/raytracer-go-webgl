{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "$id": "ray-tracer/scene.schema.json",
  "title": "Ray Tracer Scene",
  "description": "TOML scene or object library consumed by internal/sceneio. Primitives are array-of-tables (e.g. [[box]]); config blocks are single tables (e.g. [camera]).",
  "$comment": "Source of truth for the loader: internal/sceneio/toml.go. Update this file whenever the TOML format changes.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "extends": {
      "type": "string",
      "description": "Path to a base scene file (relative to this file or absolute). The base is loaded first; this file overrides [camera], [environment], [[light]], [[light_flickering]], and may add [[include]] entries or [[terrain.pad]] stubs. Used for scene variants that share most content."
    },
    "camera": {
      "$ref": "#/definitions/camera",
      "description": "TOML table [camera]. Optional starting viewpoint when the scene loads."
    },
    "player": {
      "$ref": "#/definitions/player",
      "description": "TOML table [player]. Optional per-scene player controller overrides ([player.movement])."
    },
    "environment": {
      "$ref": "#/definitions/environment",
      "description": "TOML table [environment]. Sky gradient, ambient light, and directional sun."
    },
    "include": {
      "type": "array",
      "description": "TOML array-of-tables [[include]]. Instantiates another TOML file with a placement transform.",
      "items": {
        "$ref": "#/definitions/include"
      }
    },
    "sphere": {
      "type": "array",
      "description": "TOML [[sphere]]. Spherical primitives.",
      "items": {
        "$ref": "#/definitions/sphere"
      }
    },
    "plane": {
      "type": "array",
      "description": "TOML [[plane]]. Infinite planes (half-space intersection).",
      "items": {
        "$ref": "#/definitions/plane"
      }
    },
    "box": {
      "type": "array",
      "description": "TOML [[box]]. Axis-aligned boxes; optional [[box.hole]] sub-tables cut rectangular openings.",
      "items": {
        "$ref": "#/definitions/box"
      }
    },
    "cylinder": {
      "type": "array",
      "description": "TOML [[cylinder]]. Vertical cylinders (optionally tapered with radius_top).",
      "items": {
        "$ref": "#/definitions/cylinder"
      }
    },
    "cone": {
      "type": "array",
      "description": "TOML [[cone]]. Vertical cones along Y. Base disk is capped by default; set capped = false for open foliage cones.",
      "items": {
        "$ref": "#/definitions/cone"
      }
    },
    "torus": {
      "type": "array",
      "description": "TOML [[torus]]. Ring torus in the XZ plane (major/minor radii).",
      "items": {
        "$ref": "#/definitions/torus"
      }
    },
    "ring": {
      "type": "array",
      "description": "TOML [[ring]]. Vertical annulus (washer) in the XZ plane.",
      "items": {
        "$ref": "#/definitions/ring"
      }
    },
    "lens": {
      "type": "array",
      "description": "TOML [[lens]]. Biconvex optical lens along +Y (spyglass, optics).",
      "items": {
        "$ref": "#/definitions/lens"
      }
    },
    "terrain": {
      "type": "array",
      "description": "TOML [[terrain]]. Procedural height field with optional [[terrain.feature]] hills/valleys and [[terrain.pad]] building pads.",
      "items": {
        "$ref": "#/definitions/terrain"
      }
    },
    "water": {
      "type": "array",
      "description": "TOML [[water]]. Circular water surface with ripples.",
      "items": {
        "$ref": "#/definitions/water"
      }
    },
    "light": {
      "type": "array",
      "description": "TOML [[light]]. Soft area lights (spherical emissive sources).",
      "items": {
        "$ref": "#/definitions/light"
      }
    },
    "light_flickering": {
      "type": "array",
      "description": "TOML [[light_flickering]]. Flickering warm light cluster; optional flame = true adds a procedural volumetric flame at the core.",
      "items": {
        "$ref": "#/definitions/light_flickering"
      }
    },
    "sound": {
      "type": "array",
      "description": "TOML [[sound]]. Spatial ambient audio emitters.",
      "items": {
        "$ref": "#/definitions/sound"
      }
    },
    "point": {
      "type": "array",
      "description": "TOML [[point]]. Named locations in scene space (player spawn, future NPC targets, etc.).",
      "items": {
        "$ref": "#/definitions/point"
      }
    },
    "npc": {
      "type": "array",
      "description": "TOML array-of-tables [[npc]]. Procedural humanoid (or other YAML rig) spawns. The runtime NPC manager loads the rig, appends limb primitives to the scene, and animates locomotion when speed > 0.",
      "items": {
        "$ref": "#/definitions/npc"
      }
    },
    "door": {
      "type": "array",
      "description": "TOML [[door]]. Swinging door panels with hinge animation and E-key interaction.",
      "items": {
        "$ref": "#/definitions/door"
      }
    },
    "document": {
      "type": "array",
      "description": "TOML [[document]]. Readable paper with rasterized text on the +Z face. The runtime spawns a thin box, registers an E-key interact target, and animates pick-up/read/put-away.",
      "items": {
        "$ref": "#/definitions/document"
      }
    },
    "screen": {
      "type": "array",
      "description": "TOML [[screen]]. Interactable display with rasterized UI text. The camera animates to a head-on view when used.",
      "items": {
        "$ref": "#/definitions/screen"
      }
    },
    "props": {
      "$ref": "#/definitions/meta_table",
      "description": "TOML table [props]. Overridable parameter defaults for parameterized object files."
    },
    "const": {
      "$ref": "#/definitions/meta_table",
      "description": "TOML table [const]. Derived values (single-quoted expressions) evaluated after props merge."
    },
    "state": {
      "$ref": "#/definitions/meta_table",
      "description": "TOML table [state]. Runtime-mutable variables for reactive object files. Usable in expressions, loops, and conditions; passed to children via [[include]].state."
    }
  },
  "definitions": {
    "vec3": {
      "type": "array",
      "description": "Three-component vector [x, y, z] in world units. Write components as floats (e.g. 0.0, not 0).",
      "items": {
        "$ref": "#/definitions/number_or_expr"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "vec2": {
      "type": "array",
      "description": "Two-component vector [a, b], typically X/Z or U/V.",
      "items": {
        "$ref": "#/definitions/number_or_expr"
      },
      "minItems": 2,
      "maxItems": 2
    },
    "rgb": {
      "$ref": "#/definitions/vec3",
      "description": "Linear RGB color [r, g, b]. Components are often in 0..1 but HDR emit colors may exceed 1."
    },
    "material": {
      "type": "string",
      "enum": [
        "diffuse",
        "mirror",
        "metal",
        "glass",
        "emit",
        "checker"
      ],
      "description": "Shading model. diffuse: Lambertian; mirror: perfect reflector; metal: glossy metal (use rough); glass: refractive (use ior, transmit); emit: emissive; checker: two-color pattern (planes use albedo2)."
    },
    "texture": {
      "type": "string",
      "enum": [
        "none",
        "wood",
        "brick",
        "stone",
        "cement",
        "marble",
        "grass",
        "dirt",
        "snow",
        "wallpaper_navy",
        "wallpaper_green",
        "wallpaper_rose",
        "stone_wall",
        "capture_forward",
        "capture_left",
        "capture_right",
        "capture_up",
        "capture_down",
        "capture_backward"
      ],
      "description": "Procedural world-space texture id. Omitted or \"none\" leaves the surface untextured (albedo only). Capture_* slots are filled at runtime by the portal system."
    },
    "sky": {
      "type": "string",
      "enum": [
        "clear",
        "cloudy",
        "night_stars",
        "night_storm",
        "sunset"
      ],
      "description": "Preset sky dome and sun/moon appearance."
    },
    "on_use": {
      "type": "string",
      "enum": [
        "exit_portal",
        "door",
        "document"
      ],
      "description": "Handler invoked when the player uses (E) this interactable within use_range."
    },
    "sound_id": {
      "type": "string",
      "enum": [
        "crickets",
        "fan"
      ],
      "description": "Named ambient sound loop synthesized at load time."
    },
    "rotate_x": {
      "allOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        }
      ],
      "description": "Rotation about +X in degrees about the primitive's geometric center. Defaults to 0."
    },
    "rotate_y": {
      "allOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        }
      ],
      "description": "Rotation about +Y in degrees about the primitive's geometric center. Defaults to 0."
    },
    "rotate_z": {
      "allOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        }
      ],
      "description": "Rotation about +Z in degrees about the primitive's geometric center. Defaults to 0."
    },
    "surface_albedo": {
      "allOf": [
        {
          "$ref": "#/definitions/rgb_or_expr"
        }
      ],
      "description": "Base color or tint multiplied by procedural texture."
    },
    "surface_rough": {
      "allOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        }
      ],
      "description": "Surface roughness for glossy materials (metal, diffuse with soft highlights). 0 = mirror-smooth."
    },
    "surface_ior": {
      "allOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        }
      ],
      "description": "Index of refraction for glass. Defaults to 1.5 when omitted. On thin glass, controls enter/exit bend strength in the single-hop slab solve."
    },
    "surface_reflect": {
      "allOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        }
      ],
      "description": "Extra reflection weight blended on top of the material model (0..1 typical)."
    },
    "surface_transmit": {
      "allOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        }
      ],
      "description": "Transmission weight for glass-like surfaces (0 = opaque, 1 = fully transmissive)."
    },
    "primitive_surface": {
      "type": "object",
      "description": "Material and shading fields merged into every shaded primitive table.",
      "properties": {
        "material": {
          "$ref": "#/definitions/material_or_expr"
        },
        "albedo": {
          "$ref": "#/definitions/surface_albedo"
        },
        "albedo2": {
          "$ref": "#/definitions/rgb",
          "description": "Second color for checker material; ignored for other materials."
        },
        "rough": {
          "$ref": "#/definitions/surface_rough"
        },
        "ior": {
          "$ref": "#/definitions/surface_ior"
        },
        "texture": {
          "$ref": "#/definitions/texture_or_expr"
        },
        "reflect": {
          "$ref": "#/definitions/surface_reflect"
        },
        "transmit": {
          "$ref": "#/definitions/surface_transmit"
        },
        "thin": {
          "type": "boolean",
          "description": "On glass, treat the surface as a single sheet (default true): Fresnel reflection plus a one-hop enter/exit refraction through the slab (ior controls bend strength), without a separate back-face trace hit. Set false for thick glass with a full back-face trace. Ignored for non-glass materials."
        },
        "two_pane": {
          "type": "boolean",
          "description": "When true on glass, use the original thick-glass path: separate enter/exit trace hits at both faces (full dual-pane refraction). Default glass is thin with a ghost second-pane reflection instead. Defaults to false. Ignored for non-glass materials."
        },
        "collision": {
          "type": "boolean",
          "description": "When false, the primitive is ignored by player movement, ground height, and footstep queries. Ray tracing is unaffected. Defaults to true."
        }
      },
      "required": [
        "material"
      ],
      "additionalProperties": true
    },
    "transform_origin": {
      "description": "Rotation anchor in local space. Default \"center\" (geometric center or sub-scene bounds center for includes). Use [x,y,z] for a corner, hinge, or file origin.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "center"
          ]
        },
        {
          "$ref": "#/definitions/vec3"
        }
      ]
    },
    "primitive_transform": {
      "type": "object",
      "description": "Optional per-primitive rotation fields.",
      "properties": {
        "rotate_x": {
          "$ref": "#/definitions/rotate_x"
        },
        "rotate_y": {
          "$ref": "#/definitions/rotate_y"
        },
        "rotate_z": {
          "$ref": "#/definitions/rotate_z"
        },
        "transform_origin": {
          "$ref": "#/definitions/transform_origin"
        }
      },
      "additionalProperties": true
    },
    "box_extent_pos_size": {
      "type": "object",
      "description": "Box defined by one corner (pos_*) and positive extents along +X, +Y, +Z.",
      "properties": {
        "pos_x": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Minimum X corner."
        },
        "pos_y": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Minimum Y corner."
        },
        "pos_z": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Minimum Z corner."
        },
        "width": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Extent along +X."
        },
        "height": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Extent along +Y."
        },
        "depth": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Extent along +Z."
        }
      },
      "required": [
        "width",
        "height",
        "depth"
      ],
      "additionalProperties": false
    },
    "box_hole": {
      "description": "TOML [[box.hole]] nested under a [[box]]. Rectangular void subtracted from the parent box; should pierce fully through the faces it cuts.",
      "$ref": "#/definitions/box_extent_pos_size"
    },
    "sphere": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "center": {
              "$ref": "#/definitions/vec3",
              "description": "Sphere center in world space."
            },
            "radius": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Sphere radius."
            }
          },
          "required": [
            "center",
            "radius"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        },
        {
          "$ref": "#/definitions/interact_props"
        }
      ]
    },
    "plane": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "normal": {
              "$ref": "#/definitions/vec3",
              "description": "Plane normal (need not be unit length; normalized at load)."
            },
            "d": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Plane equation n\u00b7p = d (signed distance along normal)."
            }
          },
          "required": [
            "normal",
            "d"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        }
      ]
    },
    "box": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "pos_x": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minimum X corner."
            },
            "pos_y": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minimum Y corner."
            },
            "pos_z": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minimum Z corner."
            },
            "width": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Extent along +X."
            },
            "height": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Extent along +Y."
            },
            "depth": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Extent along +Z."
            },
            "hole": {
              "type": "array",
              "description": "Nested [[box.hole]] tables belonging to this box.",
              "items": {
                "$ref": "#/definitions/box_hole"
              }
            },
            "texture_right": {
              "$ref": "#/definitions/texture_or_expr"
            },
            "texture_left": {
              "$ref": "#/definitions/texture_or_expr"
            },
            "texture_top": {
              "$ref": "#/definitions/texture_or_expr"
            },
            "texture_bottom": {
              "$ref": "#/definitions/texture_or_expr"
            },
            "texture_front": {
              "$ref": "#/definitions/texture_or_expr"
            },
            "texture_back": {
              "$ref": "#/definitions/texture_or_expr"
            }
          },
          "required": [
            "width",
            "height",
            "depth"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        },
        {
          "$ref": "#/definitions/interact_props"
        }
      ]
    },
    "cylinder": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "pos_x": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minimum X corner of the footprint square (like [[box]] pos_x)."
            },
            "pos_y": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Bottom Y (minimum height)."
            },
            "pos_z": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minimum Z corner of the footprint square."
            },
            "width": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Diameter when uniform (also used as bottom diameter when width_bottom is omitted)."
            },
            "height": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Vertical extent along +Y."
            },
            "width_bottom": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Bottom diameter for a tapered cylinder. Defaults to width."
            },
            "width_top": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Top diameter for a tapered cylinder. Defaults to width_bottom."
            },
            "open_min": {
              "type": "boolean",
              "description": "Omit the bottom end cap (hollow tube)."
            },
            "open_max": {
              "type": "boolean",
              "description": "Omit the top end cap (hollow tube)."
            }
          },
          "required": [
            "pos_x",
            "pos_y",
            "pos_z",
            "height"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        }
      ]
    },
    "cone": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "pos_x": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minimum X corner of the base footprint square."
            },
            "pos_y": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Y of the wide base."
            },
            "pos_z": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minimum Z corner of the base footprint square."
            },
            "width": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Base diameter."
            },
            "height": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Vertical span from base to tip."
            },
            "capped": {
              "type": "boolean",
              "description": "Solid base disk. Defaults to true when omitted. Set false for open-bottom cones (e.g. pine foliage)."
            }
          },
          "required": [
            "width",
            "height"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        }
      ]
    },
    "torus": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "center": {
              "$ref": "#/definitions/vec3",
              "description": "Torus center (ring lies in XZ plane)."
            },
            "major": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Major radius (ring centerline to tube center)."
            },
            "minor": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Minor radius (tube thickness)."
            }
          },
          "required": [
            "center",
            "major",
            "minor"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        }
      ]
    },
    "ring": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "cx": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Center X."
            },
            "cy": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Center Y (mid-height of the annulus)."
            },
            "cz": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Center Z."
            },
            "radius": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Radius of the ring centerline."
            },
            "height": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Vertical extent along Y."
            }
          },
          "required": [
            "cx",
            "cy",
            "cz",
            "radius"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        }
      ]
    },
    "lens": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "cx": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Center X."
            },
            "cy": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Center Y (optical axis midpoint)."
            },
            "cz": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Center Z."
            },
            "aperture": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Clear aperture radius in the XZ plane."
            },
            "r_front": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Front (+Y) spherical cap radius of curvature."
            },
            "r_back": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Back (\u2212Y) spherical cap radius of curvature."
            },
            "thickness": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Center thickness along the optical axis."
            }
          },
          "required": [
            "cx",
            "cy",
            "cz",
            "aperture",
            "r_front",
            "r_back",
            "thickness"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_transform"
        },
        {
          "$ref": "#/definitions/primitive_surface"
        }
      ]
    },
    "terrain_feature": {
      "type": "object",
      "description": "TOML [[terrain.feature]] nested under [[terrain]]. Adds a smooth hill or valley bump to the height field.",
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "peak",
            "valley"
          ],
          "description": "peak (default): positive height raises terrain. valley: positive height is negated to carve downward."
        },
        "pos": {
          "$ref": "#/definitions/vec2",
          "description": "Feature center [x, z] in terrain-local coordinates."
        },
        "height": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Bump amplitude in world units. For valleys, a positive value is treated as negative depth."
        },
        "width": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Characteristic width of the bump. Defaults to 1 when omitted or 0."
        },
        "steepness": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Profile sharpness (higher = steeper sides). Defaults to 2 when omitted or 0."
        },
        "extend": {
          "$ref": "#/definitions/vec2",
          "description": "Anisotropic stretch [x, z] of the bump footprint. Defaults to [1, 1]."
        },
        "angle": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Rotation of the bump in degrees about Y."
        }
      },
      "required": [
        "pos"
      ],
      "additionalProperties": false
    },
    "terrain_island": {
      "type": "object",
      "description": "TOML [terrain.island]. Radially fades terrain to a seabed floor outside the walkable landmass.",
      "properties": {
        "center": {
          "$ref": "#/definitions/vec2",
          "description": "Island center [x, z] in world space."
        },
        "radius": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Full-strength land radius from center."
        },
        "margin": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Blend width beyond radius down to floor. Defaults to half of radius when omitted or 0."
        },
        "floor": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Terrain height at the outer edge (typically below sea level)."
        }
      },
      "required": [
        "radius",
        "floor"
      ],
      "additionalProperties": false
    },
    "terrain_pad": {
      "type": "object",
      "description": "TOML [[terrain.pad]] nested under [[terrain]], or stub pads in extend files. Flattens a rectangular site to a target level with a smooth margin.",
      "properties": {
        "center": {
          "$ref": "#/definitions/vec2",
          "description": "Pad center [x, z] in terrain-local coordinates."
        },
        "half": {
          "$ref": "#/definitions/vec2",
          "description": "Half-extents [half_x, half_z] of the inner flat rectangle."
        },
        "level": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Ground height inside the pad: offset above natural terrain by default, or a fixed world Y when absolute = true."
        },
        "absolute": {
          "type": "boolean",
          "description": "When true, level is a fixed world elevation. When false or omitted, level is added to natural terrain height at the pad center."
        },
        "margin": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Blend width around the pad edge where height eases to natural terrain."
        }
      },
      "required": [
        "center",
        "half"
      ],
      "additionalProperties": false
    },
    "terrain": {
      "type": "object",
      "properties": {
        "origin": {
          "$ref": "#/definitions/vec3",
          "description": "World position of the terrain corner. Only origin[0] (X) and origin[2] (Z) are used."
        },
        "size": {
          "$ref": "#/definitions/vec2",
          "description": "Footprint [size_x, size_z] in world units."
        },
        "base": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Base elevation before detail noise and features."
        },
        "detail": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Amplitude of fractal detail noise."
        },
        "detail_scale": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Spatial frequency of detail noise. Defaults to 0.1 when omitted or 0."
        },
        "step": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Height quantization step for a terraced look (0 = smooth)."
        },
        "grid_cell": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Internal sampling grid cell size for the height field mesh."
        },
        "grass": {
          "$ref": "#/definitions/texture",
          "description": "Texture on gentle slopes. Defaults to grass."
        },
        "rock": {
          "$ref": "#/definitions/texture",
          "description": "Texture on steep slopes. Defaults to stone."
        },
        "snow": {
          "$ref": "#/definitions/texture",
          "description": "Texture at high elevation/slope. Defaults to snow."
        },
        "grass_col": {
          "$ref": "#/definitions/rgb",
          "description": "Tint for grass layer. All zeros default to white (show texture as authored)."
        },
        "rock_col": {
          "$ref": "#/definitions/rgb",
          "description": "Tint for rock layer."
        },
        "snow_col": {
          "$ref": "#/definitions/rgb",
          "description": "Tint for snow layer."
        },
        "slope_lo": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Slope angle (degrees) where grass\u2192rock blend begins."
        },
        "slope_hi": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Slope angle where grass\u2192rock blend completes."
        },
        "snow_lo": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Height or blend threshold where snow begins."
        },
        "snow_hi": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Height or blend threshold where snow is fully white."
        },
        "feature": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/terrain_feature"
          }
        },
        "pad": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/terrain_pad"
          }
        },
        "island": {
          "$ref": "#/definitions/terrain_island"
        }
      },
      "additionalProperties": false
    },
    "water": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "pos": {
              "$ref": "#/definitions/vec2",
              "description": "Disk center [x, z] in world space."
            },
            "radius": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Horizontal radius of the water surface. 0 or negative = infinite ocean to the horizon."
            },
            "level": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Water surface height (Y)."
            },
            "mask": {
              "type": "boolean",
              "description": "When true, omit water wherever terrain height at (x,z) is at or above level. Defaults to true for infinite oceans (radius <= 0)."
            },
            "ripple": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Normal-map ripple strength."
            },
            "ripple_animation_speed": {
              "$ref": "#/definitions/number_or_expr",
              "description": "Speed of animated ripples (0 = static)."
            },
            "ripple_direction": {
              "$ref": "#/definitions/vec2",
              "description": "Direction [x, z] the ripples travel along."
            }
          },
          "required": [
            "pos",
            "radius",
            "level"
          ],
          "additionalProperties": true
        },
        {
          "$ref": "#/definitions/primitive_surface"
        }
      ]
    },
    "light": {
      "type": "object",
      "properties": {
        "pos": {
          "$ref": "#/definitions/vec3",
          "description": "Light center."
        },
        "color": {
          "$ref": "#/definitions/rgb",
          "description": "Emissive color (HDR allowed)."
        },
        "radius": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Physical radius of the emissive sphere (soft shadow penumbra)."
        },
        "range": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Distance falloff range (0 = infinite)."
        },
        "brightness": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Intensity multiplier applied to color at load time. Defaults to 1."
        },
        "dir": {
          "$ref": "#/definitions/vec3",
          "description": "Spot light emission axis in local space. With cone_angle, casts a directional cone instead of an omnidirectional point light."
        },
        "cone_angle": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Full outer cone angle in degrees for spot lights (requires dir)."
        },
        "interactive": {
          "type": "boolean",
          "default": false,
          "description": "When true, the player can toggle this light with the use key."
        },
        "hint": {
          "type": "string",
          "default": "lamp",
          "description": "Interaction hint shown when aiming at an interactive light."
        },
        "on_use": {
          "type": "string",
          "description": "State mutation when the player uses this light, e.g. toggle(lamp_on) or lamp_on = true. Requires [state] in a parameterized object file; uses handler state instead of light_toggle."
        }
      },
      "required": [
        "pos",
        "color"
      ],
      "additionalProperties": false
    },
    "light_flickering": {
      "type": "object",
      "description": "Animated flickering point-light cluster. With flame = true, also renders a procedural volumetric flame and heat shimmer above the core.",
      "properties": {
        "center": {
          "$ref": "#/definitions/vec3",
          "description": "Cluster centre."
        },
        "color": {
          "$ref": "#/definitions/rgb",
          "description": "Emissive tint. Defaults to warm orange if omitted."
        },
        "brightness": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Intensity scale. Defaults to 1."
        },
        "range": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Light falloff distance."
        },
        "jitter": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Positional jitter amplitude. Defaults to 0.16."
        },
        "flicker": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Brightness flicker amount. Defaults to 0.45."
        },
        "speed": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Flicker animation speed. Defaults to 1."
        },
        "seed": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Random seed for reproducible flicker."
        },
        "lights": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Sub-light count. Defaults to 3 (the only value currently supported by the renderer)."
        },
        "flame": {
          "type": "boolean",
          "description": "Render a procedural volumetric flame at the core (with heat shimmer above). Defaults to false."
        },
        "flame_ember": {
          "$ref": "#/definitions/rgb_or_expr",
          "description": "Linear HDR RGB at the base of the volumetric flame. Defaults to warm red when omitted."
        },
        "flame_mid": {
          "$ref": "#/definitions/rgb_or_expr",
          "description": "Linear HDR RGB for mid-life particles. Defaults to orange when omitted."
        },
        "flame_tip": {
          "$ref": "#/definitions/rgb_or_expr",
          "description": "Linear HDR RGB for particles near the tip before fade. Defaults to yellow when omitted."
        },
        "flame_ash": {
          "$ref": "#/definitions/rgb_or_expr",
          "description": "Linear HDR RGB for particles at end of life. Defaults to dim red-brown when omitted."
        },
        "flame_scale": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Volumetric flame size multiplier (1 = default campfire scale). Use ~0.25–0.35 for a handheld torch."
        }
      },
      "required": [
        "center"
      ],
      "additionalProperties": false
    },
    "sound": {
      "type": "object",
      "properties": {
        "sound": {
          "$ref": "#/definitions/sound_id"
        },
        "at": {
          "$ref": "#/definitions/vec3",
          "description": "Emitter position."
        },
        "gain": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Volume scale. Defaults to 0.3."
        },
        "radius": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Audible falloff radius. Defaults to 20."
        }
      },
      "required": [
        "sound",
        "at"
      ],
      "additionalProperties": false
    },
    "camera": {
      "type": "object",
      "properties": {
        "pos": {
          "$ref": "#/definitions/vec3",
          "description": "Starting camera position (eye)."
        },
        "yaw": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Horizontal look angle in degrees. Forward at yaw 0 is \u2212Z (same convention as NPC heading and player movement)."
        },
        "pitch": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Vertical look angle in degrees (negative = look down)."
        }
      },
      "required": [
        "pos"
      ],
      "additionalProperties": false
    },
    "player": {
      "type": "object",
      "description": "Per-scene player controller tuning. Fields under movement override the global player.toml defaults when the scene loads.",
      "properties": {
        "movement": {
          "$ref": "#/definitions/player_movement"
        }
      },
      "additionalProperties": false
    },
    "player_movement": {
      "type": "object",
      "description": "TOML table [player.movement]. Same fields as player.toml [movement]; omitted values inherit from the global player config.",
      "properties": {
        "walk_speed": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "eye_height": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "jump_velocity": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "gravity": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "collision_radius": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "step_height": {
          "type": "number",
          "minimum": 0
        },
        "sprint_multiplier": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "crouch_eye_height": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "crouch_speed_multiplier": {
          "type": "number",
          "minimum": 0,
          "exclusiveMinimum": true
        },
        "double_jump_enabled": {
          "type": "boolean",
          "description": "Allow one extra jump while airborne (release and press jump again mid-air)."
        },
        "jolt_physics": {
          "type": "boolean",
          "description": "Drive player movement with Jolt CharacterVirtual instead of analytic scene collision."
        }
      },
      "additionalProperties": false
    },
    "sun": {
      "type": "object",
      "description": "Visible sun/moon disc appearance. Position comes from environment.sun_dir.",
      "properties": {
        "color": {
          "$ref": "#/definitions/rgb",
          "description": "Disc color."
        },
        "size": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Angular diameter in degrees."
        },
        "glow": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Halo strength around the disc. Omitted or 0 defaults to 1 when sun is configured."
        }
      },
      "additionalProperties": false
    },
    "environment": {
      "type": "object",
      "properties": {
        "sky": {
          "$ref": "#/definitions/sky"
        },
        "ambient_sky": {
          "$ref": "#/definitions/rgb",
          "description": "Hemisphere ambient from above."
        },
        "ambient_ground": {
          "$ref": "#/definitions/rgb",
          "description": "Hemisphere ambient from below."
        },
        "sun_dir": {
          "$ref": "#/definitions/vec3",
          "description": "Direction toward the sun (normalized at load). Zero vector disables directional sun."
        },
        "sun_color": {
          "$ref": "#/definitions/rgb",
          "description": "Directional sunlight color."
        },
        "sun": {
          "$ref": "#/definitions/sun"
        }
      },
      "additionalProperties": false
    },
    "interact_props": {
      "type": "object",
      "description": "Optional E-key interaction on this primitive. The player must look at the geometry (ray hit) within use_range metres.",
      "properties": {
        "hint": {
          "type": "string",
          "description": "HUD prompt shown when the crosshair hits this object (e.g. \"door\")."
        },
        "on_use": {
          "$ref": "#/definitions/on_use"
        },
        "use_range": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Maximum ray distance from the camera to the hit point."
        }
      },
      "additionalProperties": true
    },
    "point": {
      "type": "object",
      "description": "Named anchor in scene space.",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique name referenced by game code (portals, NPC goals, etc.)."
        },
        "pos": {
          "$ref": "#/definitions/vec3",
          "description": "Anchor position. For player spawn with floor_y, X/Z are horizontal and Y is ignored."
        },
        "floor_y": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Optional walkable surface height for player spawn (adds eye height above this Y)."
        },
        "yaw": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Optional initial look yaw for player spawn."
        },
        "pitch": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Optional initial look pitch for player spawn."
        }
      },
      "required": [
        "id",
        "pos"
      ],
      "additionalProperties": false
    },
    "npc": {
      "type": "object",
      "description": "TOML [[npc]]. Procedural character spawn (YAML rig + runtime pose/locomotion).",
      "properties": {
        "rig": {
          "type": "string",
          "minLength": 1,
          "description": "Path to a YAML rig file (relative to repo root or absolute), e.g. data/rigs/humanoid.yaml."
        },
        "pose": {
          "type": "string",
          "description": "Named pose from the rig YAML (e.g. idle). Default: idle. Upper body switches to walk when speed > 0."
        },
        "at": {
          "$ref": "#/definitions/vec3",
          "description": "Spawn anchor [x, y, z]. Y is feet height; 0 uses ground height at x,z."
        },
        "yaw": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Root facing yaw in degrees (same convention as [camera].yaw: 0 = \u2212Z, 90 = \u2212X, 270 = +X)."
        },
        "speed": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Locomotion speed in m/s. 0 = idle (planted feet, no hip advance)."
        },
        "heading": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Walk direction in degrees (same as yaw convention). When omitted, defaults to yaw. Ignored when patrol or goal is set."
        },
        "patrol": {
          "type": "array",
          "description": "Closed-loop patrol waypoints as [x, y, z] arrays. Y is an optional feet-height hint (0 = sample ground at runtime). Mutually exclusive with goal.",
          "items": {
            "$ref": "#/definitions/vec3"
          },
          "minItems": 1
        },
        "goal": {
          "$ref": "#/definitions/vec3",
          "description": "Single navigation target [x, y, z]. Y is an optional feet-height hint (0 = sample ground at runtime). Agent stops within ~0.2 m horizontally. Mutually exclusive with patrol."
        }
      },
      "required": [
        "rig",
        "at"
      ],
      "additionalProperties": false
    },
    "door": {
      "type": "object",
      "description": "TOML [[door]]. Swinging or sliding door with dynamic panel box(es). Optional hint and use_range set the E-key prompt and max look distance.",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique door name for interaction and debugging."
        },
        "kind": {
          "type": "string",
          "enum": [
            "single",
            "double",
            "sliding"
          ],
          "description": "Single panel, paired double doors, or sliding panel. Default: single."
        },
        "hinge": {
          "$ref": "#/definitions/vec3",
          "description": "Hinge edge position for single doors (or left panel on double doors)."
        },
        "hinge_left": {
          "$ref": "#/definitions/vec3",
          "description": "Left panel hinge for double doors."
        },
        "hinge_right": {
          "$ref": "#/definitions/vec3",
          "description": "Right panel hinge for double doors."
        },
        "axis": {
          "type": "string",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "description": "Rotation axis through the hinge (swing doors). Default: y."
        },
        "direction": {
          "type": "string",
          "enum": [
            "up",
            "down",
            "left",
            "right"
          ],
          "description": "Slide direction for sliding doors (object-local axes)."
        },
        "open_angle": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Maximum swing in degrees (swing doors). Default: 90."
        },
        "open_distance": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Travel distance in metres (sliding doors). Default: 2.0."
        },
        "swing": {
          "type": "string",
          "enum": [
            "one_way",
            "both"
          ],
          "description": "one_way always opens the same direction; both picks from the player side when closed."
        },
        "open_sign": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Swing direction (+1 / \u22121) for one_way doors."
        },
        "speed": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Swing speed in rad/s or slide speed in m/s. Default: 1.5."
        },
        "can_close": {
          "type": "boolean",
          "description": "When false the player can open but not close the door. Default: true."
        },
        "autoclose_timeout": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Seconds the door stays fully open before closing automatically. Omitted = disabled."
        },
        "panel_file": {
          "type": "string",
          "minLength": 1,
          "description": "Path to a TOML file with the moving panel geometry (boxes, cylinders, spheres). For single doors."
        },
        "panel_left_file": {
          "type": "string",
          "minLength": 1,
          "description": "Left leaf panel geometry file for double doors."
        },
        "panel_right_file": {
          "type": "string",
          "minLength": 1,
          "description": "Right leaf panel geometry file for double doors."
        },
        "panel_closed_angle": {
          "type": "array",
          "description": "Optional per-panel closed-pose rotation in degrees about each hinge.",
          "items": {
            "$ref": "#/definitions/number_or_expr"
          }
        },
        "hint": {
          "type": "string",
          "description": "HUD prompt when looking at the door panel. Default: door."
        },
        "use_range": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Max camera-to-hit distance for interaction. Default: 3.0."
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "document": {
      "type": "object",
      "description": "TOML [[document]]. A4-style paper with dynamic text rasterized at load time. pos_x/y/z is the anchor corner; after rotation the lowest point rests at pos_y. Text appears on the +Z face (use rotate_x = -90 for a flat desk memo). The runtime document manager spawns a centered thin box and handles read/put-away animation.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique document name for interaction and debugging. Default: document_N."
        },
        "pos_x": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Anchor corner X in the parent/include frame."
        },
        "pos_y": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Surface Y the paper rests on after rotation (lowest rotated point)."
        },
        "pos_z": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Anchor corner Z in the parent/include frame."
        },
        "width": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Paper width in meters. Default: 0.21 (A4 width)."
        },
        "height": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Paper height in meters. Default: 0.297 (A4 height)."
        },
        "depth": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Paper thickness in meters. Default: 0.002."
        },
        "rotate_x": {
          "$ref": "#/definitions/rotate_x"
        },
        "rotate_y": {
          "$ref": "#/definitions/rotate_y"
        },
        "rotate_z": {
          "$ref": "#/definitions/rotate_z"
        },
        "headline": {
          "type": "string",
          "description": "Optional headline rendered larger at the top of the page."
        },
        "paragraphs": {
          "$ref": "#/definitions/paragraphs_or_expr"
        },
        "font": {
          "type": "string",
          "description": "Font file name in assets/ (e.g. M42.ttf). Default: PixelOperator.ttf."
        },
        "font_size_px": {
          "$ref": "#/definitions/integer_or_expr",
          "description": "Body font size in pixels for the 512\u00d7512 raster. Default: 18."
        },
        "albedo": {
          "$ref": "#/definitions/surface_albedo",
          "description": "Tint multiplied with the rasterized ink and paper grain."
        },
        "on_use": {
          "type": "string",
          "description": "Optional handler id invoked when the player opens the document (after the read animation starts)."
        },
        "hint": {
          "type": "string",
          "description": "HUD prompt when looking at the paper. Default: press {{use_button}} to read."
        },
        "use_range": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Max camera-to-hit distance for interaction. Default: 1.2."
        }
      },
      "required": [
        "pos_x",
        "pos_y",
        "pos_z"
      ],
      "additionalProperties": false
    },
    "screen": {
      "type": "object",
      "description": "TOML [[screen]]. Display surface with dynamic text rasterized at load time. Text appears on the +Z face. Using it animates the camera to a head-on view.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique screen name. Default: screen_N."
        },
        "pos_x": {
          "$ref": "#/definitions/number_or_expr"
        },
        "pos_y": {
          "$ref": "#/definitions/number_or_expr"
        },
        "pos_z": {
          "$ref": "#/definitions/number_or_expr"
        },
        "width": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Panel width in meters. Default: 0.28."
        },
        "height": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Panel height in meters. Default: 0.23."
        },
        "depth": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Panel thickness in meters. Default: 0.002."
        },
        "rotate_x": {
          "$ref": "#/definitions/rotate_x"
        },
        "rotate_y": {
          "$ref": "#/definitions/rotate_y"
        },
        "rotate_z": {
          "$ref": "#/definitions/rotate_z"
        },
        "headline": {
          "type": "string"
        },
        "paragraphs": {
          "$ref": "#/definitions/paragraphs_or_expr"
        },
        "font": {
          "type": "string",
          "description": "Font file name in assets/ (e.g. M42.ttf). Default: PixelOperator.ttf."
        },
        "font_size_px": {
          "$ref": "#/definitions/integer_or_expr"
        },
        "font_color": {
          "$ref": "#/definitions/surface_albedo",
          "description": "Rasterized text color. Default: pale cyan."
        },
        "material": {
          "$ref": "#/definitions/material_or_expr",
          "description": "Surface material for the display panel. Default: emit."
        },
        "albedo": {
          "$ref": "#/definitions/surface_albedo",
          "description": "Surface tint and raster background color. Default: dark blue-gray."
        },
        "rough": {
          "$ref": "#/definitions/surface_rough"
        },
        "reflect": {
          "$ref": "#/definitions/surface_reflect"
        },
        "on_use": {
          "type": "string"
        },
        "hint": {
          "type": "string",
          "description": "HUD prompt when looking at the screen. Default: computer screen."
        },
        "use_range": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Max camera-to-hit distance for interaction. Default: 1.5."
        }
      },
      "required": [
        "pos_x",
        "pos_y",
        "pos_z"
      ],
      "additionalProperties": false
    },
    "include": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "minLength": 1,
          "description": "Path to the included TOML (relative to the parent file or absolute)."
        },
        "at": {
          "$ref": "#/definitions/vec3",
          "description": "Where transform_origin lands in the parent scene. When the parent has terrain, at.y is an offset above ground at (at.x, at.z); 0 snaps the anchor to ground (or a terrain pad at the anchor)."
        },
        "transform_origin": {
          "$ref": "#/definitions/transform_origin"
        },
        "rotate_x": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Rotation about X after placement (degrees)."
        },
        "rotate_y": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Rotation about Y after placement (degrees)."
        },
        "rotate_z": {
          "$ref": "#/definitions/number_or_expr",
          "description": "Rotation about Z after placement (degrees)."
        },
        "follow_terrain": {
          "type": "boolean",
          "description": "When true, Y is resolved in a post-pass after all terrain is merged: the include is shifted vertically so its anchor sits on the baked height field. Only this [[include]] line is snapped; nested includes without the flag move rigidly with the parent. For scattered props, set on each child include, not on layout files."
        },
        "instance": {
          "type": "boolean",
          "description": "When true, identical geometry from the included file is merged into a GPU instancing batch (TLAS/BLAS). The included file may be a layout of multiple [[include]] children; each unique mesh is instanced separately. Imposes an instance count limit."
        },
        "props": {
          "$ref": "#/definitions/meta_table",
          "description": "Properties merged into the included file's [props] table. Only these keys are passed; the parent's [props] are not inherited. Expressions (e.g. width = 'width') are evaluated in the parent env during expansion."
        }
      },
      "required": [
        "file"
      ],
      "additionalProperties": false
    },
    "number_or_expr": {
      "type": [
        "number",
        "string"
      ],
      "description": "Numeric literal or single-quoted sceneparam expression (e.g. 'width / 2', '-half', 'i * run')."
    },
    "integer_or_expr": {
      "type": [
        "integer",
        "number",
        "string"
      ],
      "description": "Integer literal or single-quoted sceneparam expression (e.g. 'font_size_px', 'steps')."
    },
    "rgb_or_expr": {
      "oneOf": [
        {
          "$ref": "#/definitions/vec3"
        },
        {
          "type": "string"
        }
      ],
      "description": "RGB array [r, g, b] or single-quoted expression referencing a vec3 prop (e.g. 'albedo')."
    },
    "param_value": {
      "description": "Literal or single-quoted expression for [props] / [const] values.",
      "oneOf": [
        {
          "$ref": "#/definitions/number_or_expr"
        },
        {
          "type": "boolean"
        },
        {
          "$ref": "#/definitions/rgb_literal"
        },
        {
          "$ref": "#/definitions/string_list"
        }
      ]
    },
    "meta_table": {
      "type": "object",
      "description": "Parameterized object table ([props] defaults or [const] derived values). Merged with [[include]] props for [props]. Single-quoted string values are expressions.",
      "additionalProperties": {
        "$ref": "#/definitions/param_value"
      }
    },
    "material_or_expr": {
      "type": "string",
      "description": "Material id (diffuse, mirror, metal, glass, emit, checker) or single-quoted sceneparam expression."
    },
    "texture_or_expr": {
      "type": "string",
      "description": "Texture id (wood, stone, none, \u2026) or single-quoted sceneparam expression."
    },
    "rgb_literal": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 3,
      "maxItems": 3,
      "description": "RGB literal [r, g, b] for [props] / [const]."
    },
    "string_list": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of strings (e.g. screen paragraphs)."
    },
    "paragraphs_or_expr": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/definitions/string_list"
        }
      ],
      "description": "Paragraph lines or single-quoted expression referencing a string-list prop."
    }
  }
}
