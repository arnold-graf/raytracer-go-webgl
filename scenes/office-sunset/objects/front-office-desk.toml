[[include]]
file = "../../objects/simple-table.toml"
transform_origin = [0, 0, 0]
at = [0.0, 0.0, 0.0]
rotate_y = 0
props = { width = 3.0, height = 0.9, depth = 1.5, material = "diffuse", albedo = [0.5, 0.5, 0.5] }


[[include]]
file = "../objects/workstation.toml"
transform_origin = [0, 0, 0]
at = [0.0, 0.9, 0.0]
rotate_y = -180
props = { headline = 'void water (int x, int y) {', font = 'RedAlert.ttf', font_size_px = "20", paragraphs = ['    if (x < 0 || x >= width || y < 0 || y >= height) {', '        return 0;', '    }', '    return water[x][y];', '}']}

[[include]]
file = "../objects/workstation-monitor.toml"
transform_origin = [0, 0, 0]
at = [0.7, 0.78, 0.0]
rotate_x = -2
rotate_y = -150
props = { screen_id = "monitor_2", headline = 'void water (int x, int y) {', font = 'RedAlert.ttf', font_size_px = "16", paragraphs = ['    if (x < 0 || x >= width || y < 0 || y >= height) {', '        return 0;', '    }', '    return water[x][y];', '}']}

[[include]]
file = "../../objects/desk-anglepoise-lamp.toml"
transform_origin = [0, 0, 0]
at = [-1.0, 0.9, 0.0]
rotate_y = -220
props = { stem_length = 0.2, brightness = 0.02, range = 10, albedo = [0.1, 0.1, 3] }

# floppy disks scattered on the desk (right side)
[[include]]
file = "../../objects/floppy-disk-3.5.toml"
at = [0.95, 0.9, 0.35]
rotate_y = 14

[[include]]
file = "../../objects/floppy-disk-3.5.toml"
at = [0.97, 0.9033, 0.38]
rotate_y = -22

[[include]]
file = "../../objects/floppy-disk-3.5.toml"
at = [1.08, 0.9, 0.52]
rotate_y = 71

[[include]]
file = "../../objects/floppy-disk-3.5.toml"
at = [0.72, 0.9, 0.58]
rotate_y = -35
props = { albedo = [0.12, 0.12, 0.13] }
